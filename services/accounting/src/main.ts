import Fastify from 'fastify';
import { createHash } from 'node:crypto';
import { Pool } from 'pg';

const app=Fastify({logger:true});
const port=Number(process.env.PORT??4004);
const databaseUrl=process.env.DATABASE_URL;
if(!databaseUrl)throw new Error('DATABASE_URL must be configured');
const pool=new Pool({connectionString:databaseUrl,max:10,connectionTimeoutMillis:5000,idleTimeoutMillis:30000});
const internalToken=process.env.ACCOUNTING_INTERNAL_TOKEN;
if(!internalToken||internalToken.length<32||internalToken.includes('CHANGE_ME'))throw new Error('ACCOUNTING_INTERNAL_TOKEN must be a real random secret of at least 32 characters');
const authorized=(request:{headers:{authorization?:string}})=>request.headers.authorization===`Bearer ${internalToken}`;
const DECIMAL=/^(?:0|[1-9]\d{0,19})(?:\.\d{1,18})?$/;
const validAmount=(v:unknown)=>typeof v==='string'&&DECIMAL.test(v)&&v!=='0'&&!/^0(?:\.0{1,18})?$/.test(v);
const validCurrency=(v:unknown)=>typeof v==='string'&&/^[A-Z0-9_]{2,16}$/.test(v);
const validId=(v:unknown)=>typeof v==='number'&&Number.isSafeInteger(v)&&v>0;

app.get('/health',async()=>({service:'accounting',status:'ok'}));
app.get('/health/db',async(_request,reply)=>{try{const r=await pool.query<{version:string}>('SELECT version FROM schema_migrations ORDER BY version DESC LIMIT 1');return{service:'accounting',database:'ok',migration:r.rows[0]?.version??null};}catch{return reply.code(503).send({service:'accounting',database:'unavailable'});}});
const guard={preHandler:async(req:any,reply:any)=>{if(!authorized(req))return reply.code(401).send({error:'unauthorized'});}};
app.get('/internal/v1/ledger/accounts/by-code/:code',guard,async(request,reply)=>{const code=decodeURIComponent((request.params as {code:string}).code);if(!code||code.length>200)return reply.code(400).send({error:'invalid_account_code'});const r=await pool.query('SELECT id,account_code,account_name,account_type,owner_identity_id,currency,status,created_at FROM ledger_accounts WHERE account_code=$1',[code]);if(!r.rows[0])return reply.code(404).send({error:'account_not_found'});return{account:r.rows[0]};});
app.get('/internal/v1/ledger/accounts',guard,async(req)=>{const q=req.query as {ownerIdentityId?:string;currency?:string;status?:string;accountCodePrefix?:string;limit?:string};const parsed=Number(q.limit??100);const limit=Math.min(Math.max(Number.isFinite(parsed)?parsed:100,1),500);const prefix=q.accountCodePrefix?.trim()||null;if(prefix&&prefix.length>200)return{accounts:[]};const r=await pool.query(`SELECT id,account_code,account_name,account_type,owner_identity_id,currency,status,created_at FROM ledger_accounts WHERE ($1::uuid IS NULL OR owner_identity_id=$1) AND ($2::text IS NULL OR currency=$2) AND ($3::text IS NULL OR status=$3) AND ($4::text IS NULL OR account_code LIKE $4 || '%') ORDER BY id LIMIT $5`,[q.ownerIdentityId??null,q.currency??null,q.status??null,prefix,limit]);return{accounts:r.rows};});
app.post('/internal/v1/ledger/accounts',guard,async(req,reply)=>{const b=(req.body??{}) as {accountCode?:string;accountName?:string;accountType?:string;ownerIdentityId?:string;currency?:string};if(!b.accountCode?.trim()||!b.accountName?.trim()||!['asset','liability','equity','revenue','expense'].includes(b.accountType??'')||!validCurrency(b.currency))return reply.code(400).send({error:'invalid_account'});try{const r=await pool.query(`INSERT INTO ledger_accounts(account_code,account_name,account_type,owner_identity_id,currency) VALUES($1,$2,$3,$4,$5) RETURNING *`,[b.accountCode.trim(),b.accountName.trim(),b.accountType,b.ownerIdentityId??null,b.currency]);return reply.code(201).send({account:r.rows[0]});}catch(e:any){if(e?.code==='23505')return reply.code(409).send({error:'account_already_exists'});throw e;}});
app.post('/internal/v1/ledger/transactions',guard,async(request,reply)=>{const body=(request.body??{}) as {referenceType?:string;referenceId?:string;operationId?:string;idempotencyKey?:string;description?:string;entries?:Array<{accountId?:number;direction?:string;amount?:string;currency?:string;metadata?:unknown}>};if(!body.referenceType||!body.referenceId||!body.idempotencyKey||!Array.isArray(body.entries)||body.entries.length<2||body.entries.length>100)return reply.code(400).send({error:'invalid_transaction'});if(body.referenceType.length>100||body.referenceId.length>200||body.idempotencyKey.length>200||(body.operationId&&body.operationId.length>200))return reply.code(400).send({error:'invalid_transaction'});const currencies=new Set<string>();for(const e of body.entries){if(!validId(e.accountId)||!['debit','credit'].includes(e.direction??'')||!validAmount(e.amount)||!validCurrency(e.currency))return reply.code(400).send({error:'invalid_entry'});currencies.add(e.currency!);}if(currencies.size!==1)return reply.code(400).send({error:'mixed_currencies_not_allowed'});const client=await pool.connect();try{await client.query('BEGIN');const requestHash=createHash('sha256').update(JSON.stringify({referenceType:body.referenceType,referenceId:body.referenceId,operationId:body.operationId??null,description:body.description?.slice(0,2000)??null,entries:body.entries})).digest('hex');const existing=await client.query('SELECT id,transaction_uuid,status,request_hash FROM journal_transactions WHERE idempotency_key=$1',[body.idempotencyKey]);if(existing.rows[0]){if(existing.rows[0].request_hash&&existing.rows[0].request_hash!==requestHash){await client.query('ROLLBACK');return reply.code(409).send({error:'idempotency_key_reused'});}await client.query('ROLLBACK');return{transaction:{id:existing.rows[0].id,transaction_uuid:existing.rows[0].transaction_uuid,status:existing.rows[0].status},idempotent:true};}const t=await client.query<{id:string;transaction_uuid:string;status:string}>('INSERT INTO journal_transactions(reference_type,reference_id,idempotency_key,description,request_hash,status) VALUES($1,$2,$3,$4,$5,\'pending\') RETURNING id,transaction_uuid,status',[body.referenceType,body.referenceId,body.operationId??null,body.idempotencyKey,body.description?.slice(0,2000)??null,requestHash]);for(const e of body.entries)await client.query('INSERT INTO journal_entries(journal_transaction_id,ledger_account_id,direction,amount,currency,metadata) VALUES($1,$2,$3,$4,$5,$6)',[t.rows[0].id,e.accountId,e.direction,e.amount,e.currency,e.metadata??{}]);await client.query('SELECT assert_journal_transaction_balanced($1)',[t.rows[0].id]);await client.query("UPDATE journal_transactions SET status='posted',posted_at=NOW() WHERE id=$1",[t.rows[0].id]);await client.query('COMMIT');return{transaction:{...t.rows[0],status:'posted'},idempotent:false};}catch(e){await client.query('ROLLBACK');request.log.error(e);return reply.code(400).send({error:e instanceof Error?e.message:'ledger_error'});}finally{client.release();}});
app.get('/internal/v1/ledger/transactions/:id',guard,async(request,reply)=>{const id=Number((request.params as {id:string}).id);if(!Number.isSafeInteger(id))return reply.code(400).send({error:'invalid_id'});const t=await pool.query('SELECT * FROM journal_transactions WHERE id=$1',[id]);if(!t.rows[0])return reply.code(404).send({error:'transaction_not_found'});const e=await pool.query('SELECT * FROM journal_entries WHERE journal_transaction_id=$1 ORDER BY id',[id]);return{transaction:t.rows[0],entries:e.rows};});
app.get('/internal/v1/ledger/transactions/by-operation/:operationId',guard,async(request,reply)=>{const operationId=decodeURIComponent((request.params as {operationId:string}).operationId);if(!operationId||operationId.length>200)return reply.code(400).send({error:'invalid_operation_id'});const t=await pool.query('SELECT * FROM journal_transactions WHERE operation_id=$1 ORDER BY id',[operationId]);const transactions=[];for(const row of t.rows){const e=await pool.query('SELECT * FROM journal_entries WHERE journal_transaction_id=$1 ORDER BY id',[row.id]);transactions.push({transaction:row,entries:e.rows});}return{transactions};});
app.get('/internal/v1/ledger/accounts/:id/balance',guard,async(request,reply)=>{const id=Number((request.params as {id:string}).id);if(!Number.isSafeInteger(id))return reply.code(400).send({error:'invalid_id'});const r=await pool.query(`SELECT a.id,a.account_code,a.account_name,a.account_type,a.currency,COALESCE(SUM(CASE WHEN t.status='posted' THEN CASE WHEN a.account_type IN ('asset','expense') THEN CASE WHEN e.direction='debit' THEN e.amount ELSE -e.amount END ELSE CASE WHEN e.direction='credit' THEN e.amount ELSE -e.amount END END ELSE 0 END),0)::text AS balance FROM ledger_accounts a LEFT JOIN journal_entries e ON e.ledger_account_id=a.id LEFT JOIN journal_transactions t ON t.id=e.journal_transaction_id AND t.status='posted' WHERE a.id=$1 AND a.status='active' GROUP BY a.id`,[id]);if(!r.rows[0])return reply.code(404).send({error:'account_not_found'});return{balance:r.rows[0]};});
app.get('/internal/v1/ledger/accounts/balances',guard,async(req,reply)=>{
  const q=req.query as {accountCodePrefix?:string;currency?:string;status?:string;limit?:string};
  const prefix=q.accountCodePrefix?.trim()||null;
  const parsed=Number(q.limit??500);
  const limit=Math.min(Math.max(Number.isFinite(parsed)?parsed:500,1),5000);
  if(!prefix||prefix.length>200)return reply.code(400).send({error:'account_code_prefix_required'});
  const r=await pool.query(`SELECT a.id,a.account_code,a.account_name,a.account_type,a.currency,a.status,
    COALESCE(SUM(CASE WHEN t.status='posted' THEN
      CASE WHEN a.account_type IN ('asset','expense')
        THEN CASE WHEN e.direction='debit' THEN e.amount ELSE -e.amount END
        ELSE CASE WHEN e.direction='credit' THEN e.amount ELSE -e.amount END
      END ELSE 0 END),0)::text AS balance
    FROM ledger_accounts a
    LEFT JOIN journal_entries e ON e.ledger_account_id=a.id
    LEFT JOIN journal_transactions t ON t.id=e.journal_transaction_id
    WHERE a.account_code LIKE $1 || '%'
      AND ($2::text IS NULL OR a.currency=$2)
      AND ($3::text IS NULL OR a.status=$3)
    GROUP BY a.id
    ORDER BY a.id
    LIMIT $4`,[prefix,q.currency??null,q.status??null,limit]);
  return{accounts:r.rows};
});
app.get('/internal/v1/ledger/accounts/:id/statement',guard,async(request,reply)=>{const id=Number((request.params as {id:string}).id);if(!Number.isSafeInteger(id))return reply.code(400).send({error:'invalid_id'});const q=request.query as {limit?:string;beforeId?:string};const parsed=Number(q.limit??100);const limit=Math.min(Math.max(Number.isFinite(parsed)?parsed:100,1),500);const r=await pool.query(`SELECT e.id,e.journal_transaction_id,e.direction,e.amount::text,e.currency,e.metadata,e.created_at,t.reference_type,t.reference_id,t.description FROM journal_entries e JOIN journal_transactions t ON t.id=e.journal_transaction_id WHERE e.ledger_account_id=$1 AND t.status='posted' AND ($2::bigint IS NULL OR e.id<$2) ORDER BY e.id DESC LIMIT $3`,[id,q.beforeId??null,limit]);return{entries:r.rows,nextCursor:r.rows.length===limit?String(r.rows.at(-1).id):null};});
app.post('/internal/v1/ledger/transactions/:id/reverse',guard,async(request,reply)=>{const id=Number((request.params as {id:string}).id);const b=(request.body??{}) as {idempotencyKey?:string;reason?:string};if(!Number.isSafeInteger(id)||!b.idempotencyKey||!b.reason?.trim()||b.idempotencyKey.length>200||b.reason.trim().length>2000)return reply.code(400).send({error:'invalid_reversal'});const client=await pool.connect();try{await client.query('BEGIN');const t=await client.query<any>('SELECT * FROM journal_transactions WHERE id=$1 FOR UPDATE',[id]);if(!t.rows[0])throw new Error('transaction_not_found');if(t.rows[0].status!=='posted')throw new Error('transaction_not_posted');const reason=b.reason.trim();const reversalHash=createHash('sha256').update(JSON.stringify({operation:'reverse',originalTransactionId:id,reason})).digest('hex');const existing=await client.query<any>('SELECT id,request_hash FROM journal_transactions WHERE idempotency_key=$1 FOR UPDATE',[b.idempotencyKey]);if(existing.rows[0]){if(existing.rows[0].request_hash!==reversalHash)throw new Error('idempotency_key_reused');const mapped=await client.query<any>('SELECT reversal_transaction_id FROM journal_reversals WHERE original_transaction_id=$1 AND reversal_transaction_id=$2',[id,existing.rows[0].id]);if(mapped.rows[0]){await client.query('ROLLBACK');return{reversalTransactionId:mapped.rows[0].reversal_transaction_id,idempotent:true};}throw new Error('idempotency_key_in_use');}const already=await client.query('SELECT reversal_transaction_id FROM journal_reversals WHERE original_transaction_id=$1',[id]);if(already.rows[0]){await client.query('ROLLBACK');return{reversalTransactionId:already.rows[0].reversal_transaction_id,idempotent:true};}const rt=await client.query<{id:string;transaction_uuid:string}>('INSERT INTO journal_transactions(reference_type,reference_id,idempotency_key,request_hash,description,status) VALUES(\'reversal\',$1,$2,$3,$4,\'pending\') RETURNING id,transaction_uuid',[String(id),b.idempotencyKey,reversalHash,`Reversal: ${reason.slice(0,1800)}`]);const entries=await client.query<any>('SELECT ledger_account_id,direction,amount,currency,metadata FROM journal_entries WHERE journal_transaction_id=$1 ORDER BY id',[id]);for(const e of entries.rows)await client.query('INSERT INTO journal_entries(journal_transaction_id,ledger_account_id,direction,amount,currency,metadata) VALUES($1,$2,$3,$4,$5,$6)',[rt.rows[0].id,e.ledger_account_id,e.direction==='debit'?'credit':'debit',e.amount,e.currency,{...(e.metadata??{}),reversalOf:id}]);await client.query('SELECT assert_journal_transaction_balanced($1)',[rt.rows[0].id]);await client.query("UPDATE journal_transactions SET status='posted',posted_at=NOW() WHERE id=$1",[rt.rows[0].id]);await client.query("UPDATE journal_transactions SET status='reversed' WHERE id=$1",[id]);await client.query('INSERT INTO journal_reversals(original_transaction_id,reversal_transaction_id,reason) VALUES($1,$2,$3)',[id,rt.rows[0].id,reason]);await client.query('COMMIT');return reply.code(201).send({reversalTransactionId:rt.rows[0].id,transactionUuid:rt.rows[0].transaction_uuid,idempotent:false});}catch(e){await client.query('ROLLBACK');request.log.error(e);return reply.code(400).send({error:e instanceof Error?e.message:'reversal_failed'});}finally{client.release();}});
app.post('/internal/v1/ledger/holds',guard,async(request,reply)=>{const b=(request.body??{}) as {accountId?:number;referenceType?:string;referenceId?:string;amount?:string;currency?:string};if(!validId(b.accountId)||!b.referenceType||!b.referenceId||!validAmount(b.amount)||!validCurrency(b.currency))return reply.code(400).send({error:'invalid_hold'});try{const r=await pool.query(`INSERT INTO ledger_holds(ledger_account_id,reference_type,reference_id,amount,currency) VALUES($1,$2,$3,$4,$5) RETURNING *`,[b.accountId,b.referenceType,b.referenceId,b.amount,b.currency]);return reply.code(201).send({hold:r.rows[0]});}catch(e:any){if(e?.code==='23505')return reply.code(409).send({error:'active_hold_exists'});throw e;}});
app.post('/internal/v1/ledger/holds/:id/release',guard,async(request,reply)=>{const id=Number((request.params as {id:string}).id);const r=await pool.query(`UPDATE ledger_holds SET status='released',released_at=NOW() WHERE id=$1 AND status='active' RETURNING *`,[id]);if(!r.rows[0])return reply.code(404).send({error:'active_hold_not_found'});return{hold:r.rows[0]};});
app.post('/internal/v1/ledger/holds/:id/capture',guard,async(request,reply)=>{const id=Number((request.params as {id:string}).id);const r=await pool.query(`UPDATE ledger_holds SET status='captured',released_at=NOW() WHERE id=$1 AND status='active' RETURNING *`,[id]);if(!r.rows[0])return reply.code(404).send({error:'active_hold_not_found'});return{hold:r.rows[0]};});
const shutdown=async()=>{await app.close();await pool.end()};process.on('SIGTERM',shutdown);process.on('SIGINT',shutdown);await app.listen({host:'0.0.0.0',port});
