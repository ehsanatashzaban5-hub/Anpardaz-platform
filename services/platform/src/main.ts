import Fastify from 'fastify';import cors from '@fastify/cors';import {Pool} from 'pg';import {registerAuthRoutes,requireAuth,ensurePlatformUser,type AuthClaims} from './auth.js';import {NewsRepository} from './repositories/news-repository.js';import {NewsService} from './services/news-service.js';import {registerContentRoutes} from './routes/content.js';import {registerForumRoutes} from './routes/forum.js';import {registerAdminRoutes} from './routes/admin.js';import {registerAdminSettingsRoutes} from './routes/admin-settings.js';import {registerControlPlaneRoutes} from './routes/control-plane.js';import {registerModerationRoutes} from './routes/moderation.js';import {registerEcosystemRoutes} from './routes/ecosystem.js';import {registerAdminEcosystemRoutes} from './routes/admin-ecosystem.js';import {registerOperationsRoutes} from './routes/operations.js';import {registerMarketDataRoutes} from './routes/market-data.js';import {registerAiRoutes} from './routes/ai.js';import {registerAdminGatewayRoutes} from './routes/admin-gateway.js';import {registerHooshRoutes} from './routes/hoosh.js';import {registerAdminHooshRoutes} from './routes/admin-hoosh.js';import {registerHooshMediaRoutes} from './routes/hoosh-media.js';import {registerAdminHooshSupportRoutes} from './routes/admin-hoosh-support.js';import {registerMarketAggregatorRoutes} from './routes/market-aggregator.js';import {registerAdminMarketRoutes} from './routes/admin-market.js';import {registerMarketCommunityRoutes} from './routes/market-community.js';import {registerMarketCompletionRoutes} from './routes/market-completion.js';import {registerUserSettingsRoutes} from './routes/user-settings.js';import {iranIpDecision,requireIranIpInProduction} from '@anpardaz/ip-region-policy';
const isProduction=process.env.NODE_ENV==='production';const requiredProduction=['DATABASE_URL','CORS_ORIGIN','IDENTITY_ISSUER','IDENTITY_PRIVATE_KEY_B64','GUEST_INTERACTION_SECRET','ANSARRAF_SERVICE_URL','ANPARDAZ_SERVICE_URL','ACCOUNTING_SERVICE_URL','ANSARRAF_INTERNAL_TOKEN','ACCOUNTING_INTERNAL_TOKEN','ANPARDAZ_INTERNAL_TOKEN','BANNER_SERVICE_URL','BANNER_INTERNAL_TOKEN','IP_GEOLOCATION_URL_TEMPLATE'];if(isProduction){for(const name of requiredProduction){const value=process.env[name];if(!value||value.includes('CHANGE_ME')||value.includes('your-web-domain.example')||value.includes('BASE64-DER-ED25519-PRIVATE-KEY')||value.includes('GENERATE_A_LONG_RANDOM_SECRET')||value.includes('generate-a-long-random-secret'))throw new Error(`Production environment variable ${name} must be configured with a real value`);}}
const trustProxy=process.env.TRUST_PROXY==='true';const app=Fastify({logger:true,trustProxy});const port=Number(process.env.PORT??4003);const databaseUrl=process.env.DATABASE_URL;const pool=databaseUrl?new Pool({connectionString:databaseUrl,max:10,connectionTimeoutMillis:5000,idleTimeoutMillis:30000}):null;if(!pool)app.log.warn('DATABASE_URL is not configured');const corsOrigins=process.env.CORS_ORIGIN?.split(',').map(v=>v.trim()).filter(Boolean)??['http://localhost:5173'];if(isProduction&&corsOrigins.some(v=>v==='*'||v.startsWith('http://localhost')||v.startsWith('http://127.0.0.1')))throw new Error('Production CORS_ORIGIN must not allow localhost or wildcard origins');await app.register(cors,{origin:corsOrigins});app.addHook('onSend',async(_r,reply)=>{reply.header('X-Content-Type-Options','nosniff');reply.header('Referrer-Policy','strict-origin-when-cross-origin');reply.header('X-Frame-Options','DENY');});
app.get('/health',async()=>({service:'platform',status:'ok'}));app.get('/health/db',async(_r,reply)=>{if(!pool)return reply.code(503).send({service:'platform',database:'not-configured'});try{const x=await pool.query<{version:string}>('SELECT version FROM schema_migrations ORDER BY version DESC LIMIT 1');return{service:'platform',database:'ok',migration:x.rows[0]?.version??null};}catch{return reply.code(503).send({service:'platform',database:'unavailable'});}});app.get('/api/v1/status',async()=>({service:'platform',apiVersion:'v1',status:'ready'}));app.addHook('onRequest',async(request,reply)=>{if(request.url.startsWith('/api/v1/')&&!request.url.startsWith('/api/v1/access/region'))await requireIranIpInProduction(request,reply);});app.get('/api/v1/access/region',async(request)=>{const d=await iranIpDecision(request);return{allowed:d.allowed,countryCode:d.countryCode,source:d.source};});
if(pool){app.decorate('platformPool',pool);registerAuthRoutes(app,pool);const news=new NewsService(new NewsRepository(pool));app.get('/api/v1/auth/me',{preHandler:requireAuth},async(request)=>{const a=(request as typeof request&{auth:AuthClaims}).auth,id=await ensurePlatformUser(pool,a),x=await pool.query('SELECT id,identity_id,email,display_name,role,status FROM platform_users WHERE id=$1',[id]);return x.rows[0]?{user:x.rows[0]}:{error:'user_not_found'};});app.get<{Querystring:{page?:string;limit?:string}}>('/api/v1/news',async(request,reply)=>{try{return await news.list(Number.parseInt(request.query.page??'1',10)||1,Number.parseInt(request.query.limit??'20',10)||20);}catch(e){request.log.error(e);return reply.code(503).send({error:'database_unavailable'});}});registerContentRoutes(app,pool);registerForumRoutes(app,pool);registerAdminRoutes(app,pool);registerAdminSettingsRoutes(app,pool);registerControlPlaneRoutes(app,pool);registerModerationRoutes(app,pool);registerEcosystemRoutes(app,pool);registerAdminEcosystemRoutes(app,pool);registerOperationsRoutes(app,pool);registerMarketDataRoutes(app,pool);registerAiRoutes(app,pool);registerMarketAggregatorRoutes(app,pool);registerAdminMarketRoutes(app,pool);registerMarketCommunityRoutes(app,pool);registerMarketCompletionRoutes(app,pool);registerUserSettingsRoutes(app,pool);registerAdminGatewayRoutes(app,pool);registerHooshRoutes(app,pool);registerAdminHooshRoutes(app,pool);registerHooshMediaRoutes(app,pool);registerAdminHooshSupportRoutes(app,pool);}
const shutdown=async()=>{await app.close();await pool?.end()};process.on('SIGTERM',shutdown);process.on('SIGINT',shutdown);await app.listen({host:'0.0.0.0',port});
